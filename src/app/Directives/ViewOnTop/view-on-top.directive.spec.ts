import { ViewOnTopDirective } from './view-on-top.directive';

describe('ViewOnTopDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewOnTopDirective();
    expect(directive).toBeTruthy();
  });
});
