import { MapKeyValuePipePipe } from './map-key-value-pipe.pipe';

describe('MapKeyValuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MapKeyValuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
