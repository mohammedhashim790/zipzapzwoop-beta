import { CopyClipboardDirectiveDirective } from './copy-clipboard-directive.directive';

describe('CopyClipboardDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyClipboardDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
