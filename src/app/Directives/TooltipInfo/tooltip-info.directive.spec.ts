import { TooltipInfoDirective } from './tooltip-info.directive';

describe('TooltipInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipInfoDirective();
    expect(directive).toBeTruthy();
  });
});
